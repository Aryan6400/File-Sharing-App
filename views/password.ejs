<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ShariFy</title>
</head>

<body>
    <form method="POST" style="display:grid; gap: 1rem; grid-template-columns: auto 1fr; max-width: 350px; margin: 75px auto;">
        <label for="password">Enter the password:</label>
        <input type="password" name="password" id="password" required>
        <button style="color: white; background-color: blue; border-radius: 5px; font-size: 1rem; border: none;" type="submit" class="btn">Download</button>
    </form>
    <% if(status!=null){ %>
        <div style="max-width: 60%; margin: 20px auto; text-align: center; font-size: 1.2rem; color: rgb(255, 51, 0);">
            <%= status %>
        </div>
    <% } %>
</body>

</html>